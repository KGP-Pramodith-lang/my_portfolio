# --- OS junk ---
.DS_Store
.AppleDouble
.LSOverride

# iCloud / Finder metadata
*.icloud

# Windows junk (harmless on macOS, useful if repo is shared)
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Environment / secrets (keep templates committed if you use them) ---
.env
.env.*
!.env.example

# --- Dependencies / build outputs (only relevant if you add a build tool later) ---
node_modules/
dist/
build/
.parcel-cache/
.vite/
.next/
out/
coverage/

# --- Editor / tooling ---
.vscode/
.idea/
*.swp